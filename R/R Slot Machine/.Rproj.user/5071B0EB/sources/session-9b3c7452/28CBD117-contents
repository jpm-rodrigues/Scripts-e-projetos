# Programando a Slot Machine

## c1. play <-function() {
## c2. symbols <- get symbols() 
## c3. print(symbols)
## c4. score(symbols
##     } 



## Passo 1: Gerar os símbolos
get_symbols <- function() {
wheel <- c("DD", "7", "BBB", "BB", "B","C", "0")
  sample(wheel, size = 3, replace = TRUE,
         prob = c(0.03, 0.03, 0.06, 0.1, 0.25, 0.1, 0.52))
}

symbols <- get_symbols()

## Passo 2: Display os símbolos

same <- lenght((unique(symbols) == 1)

if ( # Case 1: all the same <1>) {
  prize <- # look up the prize <3>
  } else if ( # Case 2: all bars <2> ) {
    prize <- # assign $5 <4>
    } else {
      # count cherries <5>
      prize <- # calculate a prize <7>
    }

# count diamonds <6>
# double the prize if necessary <8>

 
#1 - Same 
all_same <- length(unique(symbols) == 1)
all_bars <- symbols %in% c("B","BB","BBB")


#3 - Assign prizes
if (all_same) {
  payouts <- c("DD" = 100, "7" = 80, "BBB" = 40, "BB" = 25, "B" = 10, "C" = 10, "0" = 0) # Criando uma tabela com "nomes" e valores.
  prize <- unname(payouts[symbols[1]])
} else if all(all_bars) {
  prize <- 5
} else {
  cherries <- sum(symbols == "C")
  
  
  prize <- # calculate a prize
}
  

